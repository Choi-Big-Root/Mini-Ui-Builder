import 'package:flutter/material.dart';

class WidgetPropertyPanel extends StatelessWidget {
  const WidgetPropertyPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
